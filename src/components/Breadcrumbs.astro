---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<nav class="breadcrumbs" aria-label="面包屑">
  <ol>
    <li><a href="/">首页</a></li>
    <li aria-current="page">{title}</li>
  </ol>
</nav>

<style>
  .breadcrumbs ol {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.35rem;
    margin: 0;
    padding: 0;
    color: var(--hero-muted, var(--color-muted));
    font-size: 0.82rem;
    list-style: none;
  }

  .breadcrumbs li:not(:last-child)::after {
    margin-left: 0.35rem;
    color: var(--hero-muted, var(--color-muted));
    content: '/';
  }

  .breadcrumbs a:hover {
    color: var(--hero-text, var(--color-link));
    text-decoration: underline;
    text-underline-offset: 0.2em;
  }

  .breadcrumbs [aria-current='page'] {
    overflow-wrap: anywhere;
  }
</style>
