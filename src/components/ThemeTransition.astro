<div
  class="theme-transition"
  data-theme-transition
  data-from-theme=""
  data-to-theme=""
  aria-hidden="true"
  hidden
>
  <div class="theme-transition__night"></div>
  <div class="theme-transition__stars"></div>

  <div class="theme-transition__orbit">
    <span class="theme-transition__sun"></span>
    <span class="theme-transition__moon">
      <span></span>
      <span></span>
      <span></span>
    </span>
  </div>

  <div class="theme-transition__cat">
    <span class="theme-transition__eye theme-transition__eye--left">
      <span class="theme-transition__pupil"></span>
    </span>
    <span class="theme-transition__eye theme-transition__eye--right">
      <span class="theme-transition__pupil"></span>
    </span>
    <span class="theme-transition__nose"></span>
  </div>
</div>

<style>
  .theme-transition {
    position: fixed;
    z-index: 10000;
    inset: 0;
    overflow: hidden;
    background: linear-gradient(to top, #ffdca5 0%, #d9f1e8 46%, #ccebf7 100%);
    contain: strict;
    opacity: 0;
    pointer-events: none;
  }

  .theme-transition.is-active {
    pointer-events: auto;
    animation: theme-transition-enter 200ms ease-out both;
  }

  .theme-transition.is-leaving {
    pointer-events: none;
    animation: theme-transition-leave 200ms ease-in both;
  }

  .theme-transition__night,
  .theme-transition__stars {
    position: absolute;
    inset: 0;
  }

  .theme-transition__night {
    z-index: 0;
    background: linear-gradient(to top, #27b8c3 0%, #334276 55%, #171329 100%);
  }

  [data-from-theme='light'][data-to-theme='dark'] .theme-transition__night {
    animation: theme-transition-night-in 2s 410ms ease both;
  }

  [data-from-theme='dark'][data-to-theme='light'] .theme-transition__night {
    animation: theme-transition-night-out 2s 410ms ease both;
  }

  .theme-transition__stars {
    z-index: 1;
    opacity: 0;
    background-image:
      radial-gradient(circle at 8% 18%, rgb(255 255 255 / 70%) 0 1px, transparent 2px),
      radial-gradient(circle at 22% 31%, rgb(255 255 255 / 48%) 0 1px, transparent 2px),
      radial-gradient(circle at 41% 12%, rgb(255 255 255 / 58%) 0 1px, transparent 2px),
      radial-gradient(circle at 67% 24%, rgb(255 255 255 / 66%) 0 1px, transparent 2px),
      radial-gradient(circle at 86% 14%, rgb(255 255 255 / 52%) 0 1px, transparent 2px),
      radial-gradient(circle at 94% 36%, rgb(255 255 255 / 60%) 0 1px, transparent 2px);
  }

  [data-from-theme='light'][data-to-theme='dark'] .theme-transition__stars {
    animation: theme-transition-stars-in 1.4s 300ms ease both;
  }

  [data-from-theme='dark'][data-to-theme='light'] .theme-transition__stars {
    animation: theme-transition-stars-out 1.4s ease both;
  }

  .theme-transition__orbit {
    position: fixed;
    z-index: 2;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    transform-origin: center bottom;
  }

  .is-active .theme-transition__orbit {
    animation: theme-transition-celestial-track 2s cubic-bezier(0.7, 0, 0, 1) both;
  }

  .theme-transition__sun,
  .theme-transition__moon {
    position: absolute;
    top: 32%;
    left: 55%;
    display: block;
    border-radius: 50%;
  }

  .theme-transition__sun {
    width: 40px;
    aspect-ratio: 1;
    background: #ffe98a;
    box-shadow: 0 0 34px rgb(255 233 138 / 85%);
  }

  .theme-transition__moon {
    width: 24px;
    aspect-ratio: 1;
    overflow: hidden;
    background: #f7f5e9;
    box-shadow: 0 0 28px rgb(255 255 255 / 88%);
  }

  .theme-transition__moon span {
    position: absolute;
    display: block;
    border-radius: 50%;
    background: rgb(173 179 206 / 42%);
  }

  .theme-transition__moon span:nth-child(1) {
    top: 18%;
    left: 18%;
    width: 24%;
    height: 24%;
  }

  .theme-transition__moon span:nth-child(2) {
    right: 15%;
    bottom: 20%;
    width: 34%;
    height: 34%;
  }

  .theme-transition__moon span:nth-child(3) {
    bottom: 12%;
    left: 18%;
    width: 16%;
    height: 16%;
  }

  [data-to-theme='dark'] .theme-transition__sun,
  [data-to-theme='light'] .theme-transition__moon {
    opacity: 0;
  }

  [data-to-theme='dark'] .theme-transition__moon,
  [data-to-theme='light'] .theme-transition__sun {
    opacity: 1;
  }

  .theme-transition__cat {
    --pupil-slit-width: clamp(4px, 0.208333vw, 8px);
    --pupil-slit-height: clamp(30px, 1.5625vw, 60px);
    --pupil-open-size: clamp(27px, 1.40625vw, 54px);
    position: absolute;
    z-index: 3;
    bottom: clamp(-40px, -1.25vw, -24px);
    left: 50%;
    width: clamp(108px, 5.3125vw, 204px);
    height: auto;
    aspect-ratio: 108 / 132;
    border-radius: 0;
    background: #777;
    box-shadow: none;
    clip-path: polygon(0 0, 22% 12%, 78% 12%, 100% 0, 100% 100%, 0 100%);
    transform: translateX(-50%);
    transition: background-color 500ms ease;
  }

  .is-active .theme-transition__cat {
    animation: theme-transition-cat-rise 700ms cubic-bezier(0.2, 0.8, 0.2, 1) both;
  }

  [data-to-theme='dark'] .theme-transition__cat {
    background: #444;
  }

  .theme-transition__eye {
    position: absolute;
    top: clamp(30px, 1.484375vw, 57px);
    display: grid;
    width: clamp(32px, 1.5625vw, 60px);
    aspect-ratio: 1;
    place-items: center;
    overflow: hidden;
    border-radius: 50%;
    background: #ffee94;
  }

  .theme-transition__eye--left {
    left: clamp(10px, 0.494792vw, 19px);
  }

  .theme-transition__eye--right {
    right: clamp(10px, 0.494792vw, 19px);
  }

  .theme-transition__pupil {
    display: block;
    width: var(--pupil-slit-width);
    height: var(--pupil-slit-height);
    border-radius: 50%;
    background: #ffb399;
  }

  [data-from-theme='light'][data-to-theme='dark'] .theme-transition__pupil {
    animation: theme-transition-pupil-open 1s 420ms ease both;
  }

  [data-from-theme='dark'][data-to-theme='light'] .theme-transition__pupil {
    animation: theme-transition-pupil-close 1s 250ms ease both;
  }

  .theme-transition__nose {
    position: absolute;
    top: clamp(50px, 2.604167vw, 100px);
    left: 50%;
    width: clamp(8px, 0.416667vw, 16px);
    aspect-ratio: 1;
    border-radius: 45% 45% 55% 55%;
    background: #ffb399;
    transform: translateX(-50%) rotate(45deg);
  }

  @keyframes theme-transition-enter {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes theme-transition-leave {
    from { opacity: 1; }
    to { opacity: 0; }
  }

  @keyframes theme-transition-night-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes theme-transition-night-out {
    from { opacity: 1; }
    to { opacity: 0; }
  }

  @keyframes theme-transition-stars-in {
    from { opacity: 0; }
    to { opacity: 0.7; }
  }

  @keyframes theme-transition-stars-out {
    from { opacity: 0.7; }
    to { opacity: 0; }
  }

  @keyframes theme-transition-celestial-track {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  @keyframes theme-transition-cat-rise {
    0% { transform: translate(-50%, 120%); }
    70% { transform: translate(-50%, -4%); }
    100% { transform: translate(-50%, 0); }
  }

  @keyframes theme-transition-pupil-open {
    from { width: var(--pupil-slit-width); height: var(--pupil-slit-height); }
    to { width: var(--pupil-open-size); height: var(--pupil-open-size); }
  }

  @keyframes theme-transition-pupil-close {
    from { width: var(--pupil-open-size); height: var(--pupil-open-size); }
    to { width: var(--pupil-slit-width); height: var(--pupil-slit-height); }
  }

  @media (max-width: 480px) {
    .theme-transition__cat {
      --pupil-slit-width: 4px;
      --pupil-slit-height: 26px;
      --pupil-open-size: 23px;
      bottom: -20px;
      width: 92px;
      height: 118px;
    }

    .theme-transition__eye {
      top: 28px;
      width: 28px;
    }

    .theme-transition__eye--left {
      left: 9px;
    }

    .theme-transition__eye--right {
      right: 9px;
    }

    .theme-transition__nose {
      top: 47px;
      width: 7px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .theme-transition,
    .theme-transition * {
      animation: none !important;
      transition: none !important;
    }
  }
</style>
